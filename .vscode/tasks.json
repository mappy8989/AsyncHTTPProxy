{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "GCC: Build Debug app",
            "type": "shell",
            "command": [
                "mkdir ${workspaceFolder}/build ; ",
                "cd ${workspaceFolder}/build &&",
                "make -j4"
            ],
            "presentation": {
                "clear": true
            },
            "hide": false,
            "group": {
                "kind": "build"
            }
        }
    ]
}
